description: Not Found. Contact with the given id does not exist.
content:
  application/json:
    schema:
      type: object
      properties:
        status:
          type: number
          example: 404
        message:
          type: string
          example: 'Contact not found'
        data:
          type: object
          properties:
            message:
              type: string
              example: 'Contact not found'
